{
  "kind": "collectionType",
  "collectionName": "morpho_vault",
  "info": {
    "singularName": "morpho-vaults",
    "pluralName": "morpho-vault",
    "displayName": "MorphoVaults"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "chain": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::chain.chain",
      "inversedBy": "morpho_vaults"
    },
    "contract": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::contract.contract",
      "inversedBy": "morpho_vaults"
    },
    "StakingManager": {
      "type": "string",
      "minLength": 42,
      "maxLength": 42,
      "required": true,
      "unique": true,
      "regex": "^0x[a-fA-F0-9]{40}$"
    },
    "MorphoVault": {
      "type": "string",
      "minLength": 42,
      "maxLength": 42,
      "required": true,
      "unique": true,
      "regex": "^0x[a-fA-F0-9]{40}$"
    }
  }
}
